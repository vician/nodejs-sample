apiVersion: route.openshift.io/v1
kind: Route
metadata:
  labels:
    app: nodejs-app
  name: my-nodejs-route
spec:
  port:
    targetPort: 3001-tcp
  tls: 
    insecureEdgeTerminationPolicy: Redirect
    termination: edge    
  to:
    kind: Service
    name: my-nodejs-svc
    weight: 100
